THRESHOLDS = {
    "review_overload_pct": 60.0,      # alert if needs_review_pct > this
    "no_data_pct": 10.0,              # alert if missing data rate > this
    "concentration_pct": 50.0,        # alert if top 20% share >= this
}